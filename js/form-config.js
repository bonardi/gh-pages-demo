// configuration file for subscription/signup form
// set `enabled` to true to show the embed form or false to disable it.
const signupConfig = {
    enabled: true // flip to false to hide the form
};